{
  "version": 2,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/client/dist/index.html"
    }
  ],
  "env": {
    "PREBUILD_SCRIPT": "chmod +x ./node_modules/.bin/vite && npm install"
  }
}
